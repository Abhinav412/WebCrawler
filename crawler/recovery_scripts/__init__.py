"""Recovery scripts — auto-generated Python Playwright scrapers saved by the ReAct Investigator.

Each file in this directory is a self-contained async function with the naming
convention `<sanitized-domain>.py`, e.g. `t_hub_co.py`.

When the mass crawler fails to scrape a domain, it checks this directory first.
If a script exists, it runs it directly without launching an LLM session.
"""
